language: node_js
node_js:
  - "8"
  - "10"
  - "12"
  - "14"

cache: yarn

install:
  - yarn install

script:
  - yarn test

jobs:
  include:
    - stage: npm release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script: "echo '//registry.npmjs.org/:_authToken=${NPM_TOKEN}' >> .npmrc && npm publish"
        on:
          branch: master
